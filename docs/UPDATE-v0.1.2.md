# v0.1.2

- 删除正式启动页 Demo/Test Only 卡片。
- 保留空白 workspace，不自动进入测试世界。
- 新增 attributes / aptitudes / skills / traits / equipment / quests 六个通用模块。
- 新增属性、资质、技能、特质、装备、任务 UI 面板。
- 新增 EQUIP / UNEQUIP，校验背包、槽位、重复装备引用。
- `GAME_WORLD_FILE` / `PROMPT_PROFILE_PATH` 启动配置接线完成。
- UI 版本标识更新为 v0.1.2；package version 更新。
- Imported RPG data can use the structured components rather than relying only on prompt context.
