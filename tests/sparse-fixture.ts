import {demo,MemoryStore} from './helpers.js';import {GameService} from '../src/server/service.js';import {AIRuntime} from '../src/ai/runtime.js';import type {AIRequest} from '../src/ai/contracts.js';import type {WorldPackage} from '../src/core/schema.js';import type {RoutinePlan} from '../src/routine/schema.js';
export const sparsePlan:RoutinePlan={version:1,blocks:[{id:'sleep',weekdays:[1,2,3,4,5,6,7],start_minute:0,activity_id:'sleep',candidate_ids:[]},{id:'course',weekdays:[1,2,3,4,5,6,7],start_minute:540,activity_id:'course',candidate_ids:[]},{id:'training',weekdays:[1,2,3,4,5,6,7],start_minute:780,activity_id:'training',candidate_ids:[]},{id:'work',weekdays:[1,2,3,4,5,6,7],start_minute:960,activity_id:'work',candidate_ids:[]}],interrupt_policy:['重要事件立即暂停'],applied_assumptions:[],source_revision:0,clarification:null};
export function sparseWorld():WorldPackage {const w=structuredClone(demo);w.events=[];w.runtime.time={day:1,minute:0};w.enabled_modules.push('attributes','aptitudes','skills');
w.calendar={version:1,anchor_day:1,anchor_month:1,anchor_date:1,anchor_weekday:1,month_lengths:[31,28,31,30,31,30,31,31,30,31,30,31],source:'明确创作的测试世界日历'};
 const p=w.entities.find(e=>e.id==='player')!;p.components.condition={hp:100,stamina:50,stress:20};p.components.skills={entries:{study:{name:'学习',xp:0,level:1,next_xp:10000,aptitude_ref:null,tags:[]}}};
 w.routine_rules={version:1,activities:[{id:'sleep',label:'夜间睡眠',kind:'sleep',duration:480,location_id:'square',mode:'local',effects:[{op:'condition_delta',key:'stamina',target_id:null,delta:16,reason:'明确的世界睡眠恢复规则'}],source:'author-defined recovery rule'}, {id:'course',label:'固定课程',kind:'course',duration:180,location_id:'station',mode:'local',effects:[{op:'skill_xp',key:'study',target_id:null,delta:2,reason:'明确的每课成长规则'}],source:'author-defined class rule'}, {id:'training',label:'固定训练',kind:'training',duration:120,location_id:'station',mode:'local',effects:[{op:'skill_xp',key:'study',target_id:null,delta:1,reason:'明确的每次训练成长规则'}],source:'author-defined training rule'}, {id:'work',label:'固定兼职',kind:'work',duration:120,location_id:'station',mode:'local',effects:[{op:'wallet_delta',key:'credit',target_id:null,delta:8,reason:'已确认的两小时工作报酬'}],source:'author-defined wage rule'}]};return w;}
export async function sparseSetup(generate?:(r:AIRequest)=>Promise<any>){const calls:AIRequest[]=[];const store=new MemoryStore();const ai=new AIRuntime({name:'fixture',generate:async req=>{calls.push(req);if(generate)return generate(req);if(req.role==='routine_compiler')return {data:sparsePlan};throw Error('unexpected AI call on ordinary path');}});const service=new GameService(store,ai,sparseWorld());await service.newGame();await service.current();return {service,store,ai,calls};}
